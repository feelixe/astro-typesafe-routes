---
import { z } from "zod";
import Link from "astro-typesafe-routes/link";

export const searchSchema = z.object({
  filter: z.string(),
  isActive: z.boolean(),
});
---

<Link to="/" search={{ filter: "hej", isActive: true }} />
<div>hej!</div>
