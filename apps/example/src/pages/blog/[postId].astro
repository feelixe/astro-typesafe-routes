---
import { createRoute } from "astro-typesafe-routes/create-route";
import { z } from "astro/zod";

const route = createRoute(Astro, {
  routeId: "/blog/[postId]",
  searchSchema: z.object({
    name: z.string().optional(),
  }),
});

route.params.postId;
route.search.name;
---

postid
