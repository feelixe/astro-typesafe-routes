---
import { createRoute } from "astro-typesafe-routes/route-schema";

const route = createRoute({
  routeId: "/blog/[postId]",
});

export const getStaticPaths = route.createGetStaticPaths(() => {
  return [
    {
      params: { postId: "hej" },
    },
  ];
});

route.rewrite(Astro, { to: "/blog/[postId]", params: { postId: "1" } });
---

123
